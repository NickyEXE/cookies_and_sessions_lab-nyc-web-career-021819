<% if @cart%>
  <% @cart.each do |item| %>
    <%= item %>
  <% end %>
  <% else %>
  <%= @cart %>
<% end %>

<%= form_tag '/', method: :post do %>
  <%= label_tag(:product, "Add an item to cart:") %>
  <%= text_field_tag(:product) %>
  <%= submit_tag("add to cart") %>
<% end %>
